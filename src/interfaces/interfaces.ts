export interface ITodo {
  id: number;
  title: string;
  completed: boolean;
}

export interface IUsers {
  title: string;
  completed: boolean;
  id: number;
}

export interface IUser {
  id: number;
  name: string;
  email: string;
}
